# used by `watchpost check --alert` to notify on down/recovery
# works with either a Slack incoming webhook or a Discord webhook url
WATCHPOST_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url
