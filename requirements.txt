# core
requests
pyyaml
pytest

# optional: needed for `check --alert` (webhook notifications) and
# `check --diagnose` (LLM failure diagnosis) - both read .env
# python-dotenv

# optional: needed for `check --diagnose`
# anthropic
